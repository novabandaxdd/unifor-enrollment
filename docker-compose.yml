services:

  postgres:
    image: postgres:16-alpine
    container_name: unifor-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: unifor_db
      POSTGRES_USER: unifor
      POSTGRES_PASSWORD: unifor_pass
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./infra/postgres/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U unifor -d unifor_db"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s

  keycloak:
    image: quay.io/keycloak/keycloak:24.0
    container_name: unifor-keycloak
    restart: unless-stopped
    command: start-dev --import-realm
    environment:
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: admin
      KC_DB: dev-file
    ports:
      - "8180:8080"
    volumes:
      - ./infra/keycloak:/opt/keycloak/data/import:ro
    depends_on:
      postgres:
        condition: service_healthy
    # No healthcheck needed — backend uses restart:unless-stopped to retry
    # Keycloak start-dev initialises realm import before accepting connections

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    image: unifor-enrollment-backend
    container_name: unifor-backend
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      # Datasource
      QUARKUS_DATASOURCE_JDBC_URL: jdbc:postgresql://postgres:5432/unifor_db
      QUARKUS_DATASOURCE_USERNAME: unifor
      QUARKUS_DATASOURCE_PASSWORD: unifor_pass
      # Hibernate — validate schema against init.sql DDL
      QUARKUS_HIBERNATE_ORM_DATABASE_GENERATION: validate
      QUARKUS_HIBERNATE_ORM_SQL_LOAD_SCRIPT: no-file
      # OIDC — points to keycloak service name inside Docker network
      QUARKUS_OIDC_AUTH_SERVER_URL: http://keycloak:8080/realms/unifor
      QUARKUS_OIDC_CLIENT_ID: unifor-backend
      QUARKUS_OIDC_CREDENTIALS_SECRET: unifor-backend-secret
      QUARKUS_OIDC_TLS_VERIFICATION: none
      # CORS — aceita chamadas do Angular dev local E do nginx do Docker
      QUARKUS_HTTP_CORS: "true"
      QUARKUS_HTTP_CORS_ORIGINS: "http://localhost:4200,http://localhost:80,http://localhost"
      QUARKUS_HTTP_CORS_METHODS: GET,POST,PUT,PATCH,DELETE,OPTIONS
      QUARKUS_HTTP_CORS_HEADERS: Content-Type,Authorization,Accept,X-Requested-With
      # Logging
      QUARKUS_LOG_LEVEL: INFO
    depends_on:
      postgres:
        condition: service_healthy
      keycloak:
        condition: service_started

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    image: unifor-enrollment-frontend
    container_name: unifor-frontend
    restart: unless-stopped
    ports:
      - "4200:80"
    depends_on:
      - backend

volumes:
  postgres_data:
