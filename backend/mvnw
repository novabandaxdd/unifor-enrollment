#!/bin/sh
# Maven Wrapper script for Unix-like systems
# This is a minimal shell wrapper; replace with the full Maven wrapper
# generated by `mvn wrapper:wrapper` if desired.

set -e
MAVEN_OPTS="${MAVEN_OPTS:--Xmx512m}"
exec mvn "$@"
